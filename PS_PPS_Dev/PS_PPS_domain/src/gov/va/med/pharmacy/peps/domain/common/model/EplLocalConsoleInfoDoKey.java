/**
 *  Copyright 2008, Southwest Research Institute
 */


package gov.va.med.pharmacy.peps.domain.common.model;


/**
 * EplLocalConsoleInfoDoKey generated by hbm2java
 * @hibernate.class
 */
public class EplLocalConsoleInfoDoKey implements java.io.Serializable {
    private static final long serialVersionUID = 1L;

    // Fields

    private String siteNumber;
    private String softwareUpdateType;

    // Constructors

    /** default constructor */

    public EplLocalConsoleInfoDoKey() {
    }

    /** full constructor */

    public EplLocalConsoleInfoDoKey(String siteNumber, String softwareUpdateType) {
        this.siteNumber = siteNumber;
        this.softwareUpdateType = softwareUpdateType;
    }

    // Property accessors
    public String getSiteNumber() {
        return this.siteNumber;
    }

    public void setSiteNumber(String siteNumber) {
        this.siteNumber = siteNumber;
    }

    public String getSoftwareUpdateType() {
        return this.softwareUpdateType;
    }

    public void setSoftwareUpdateType(String softwareUpdateType) {
        this.softwareUpdateType = softwareUpdateType;
    }

    public boolean equals(Object other) {
        if ((this == other)) {
            return true;
        }

        if ((other == null)) {
            return false;
        }

        if (!(other instanceof EplLocalConsoleInfoDoKey)) {
            return false;
        }

        EplLocalConsoleInfoDoKey castOther = (EplLocalConsoleInfoDoKey) other;

        return ((this.getSiteNumber() == castOther.getSiteNumber()) || (this.getSiteNumber() != null
            && castOther.getSiteNumber() != null && this.getSiteNumber().equals(castOther.getSiteNumber())))
            && ((this.getSoftwareUpdateType() == castOther.getSoftwareUpdateType()) || (this.getSoftwareUpdateType() != null
                && castOther.getSoftwareUpdateType() != null && this.getSoftwareUpdateType().equals(
                    castOther.getSoftwareUpdateType())));
    }

    public int hashCode() {
        int result = 17;

        result = 37 * result + (getSiteNumber() == null ? 0 : this.getSiteNumber().hashCode());
        result = 37 * result + (getSoftwareUpdateType() == null ? 0 : this.getSoftwareUpdateType().hashCode());

        return result;
    }

}
